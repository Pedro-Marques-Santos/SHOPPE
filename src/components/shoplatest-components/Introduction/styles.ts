import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  justify-content: space-between;
`;

export const Title = styled.div`
  color: var(--gold-black);
  font-size: 22px;
  font-weight: 500;
  line-height: 43px;
  letter-spacing: 0em;
`;

export const AllElements = styled.a`
  display: flex;
  align-items: center;
  text-decoration: none;
  color: var(--gold-black);
  font-size: 15px;
  font-weight: 400;
  line-height: 26px;
  letter-spacing: 0em;
`;
